.ui.text.container
  table.ui.celled.table.selectable.pointer
    thead
      tr
        th ID
        th Problem Name
    tbody
      tr( v-for="x of problems" v-link="{ name: 'problem', params: { id: x._id }}")
        td {{ x._id }}
        td {{ x.name }}

