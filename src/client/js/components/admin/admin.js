import Vue from 'vue';
import template from './admin.pug';

export default Vue.extend({
    template,
    data() {
        return { 
            problems: [],
        };
    },
    ready() {
    },
});
